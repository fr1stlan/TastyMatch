<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#A5AF4B"
    android:clickable="true"
    tools:context=".AddFragment">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/add_image"
            android:layout_width="320dp"
            android:layout_height="180dp"
            android:layout_marginHorizontal="45dp"
            android:layout_marginTop="40sp"
            android:background="@drawable/item_bg"
            android:src="@drawable/baseline_image_24" />

        <EditText
            android:id="@+id/add_editTitle"
            android:layout_width="match_parent"
            android:layout_height="48sp"
            android:layout_below="@+id/add_image"
            android:layout_marginHorizontal="20sp"
            android:layout_marginTop="10dp"
            android:hint="Название блюда..."
            android:textColorHint="@color/hint" />

        <Spinner
            android:id="@+id/add_spinner"
            android:layout_width="match_parent"
            android:layout_height="48sp"
            android:layout_below="@+id/add_editTitle"
            android:layout_marginHorizontal="20sp"
            android:layout_marginTop="10sp" />

        <ExpandableListView
            android:id="@+id/add_expandanbleList"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/add_spinner"
            android:layout_marginHorizontal="20sp"
            android:layout_marginTop="20sp" />

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/add_expandanbleList"
            android:layout_marginHorizontal="25dp">

            <Button
                android:id="@+id/add_saveButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Сохранить" />

            <Button
                android:id="@+id/add_resetButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="130dp"
                android:layout_toEndOf="@+id/add_saveButton"
                android:text="Сброс" />

        </RelativeLayout>

    </RelativeLayout>

</FrameLayout>